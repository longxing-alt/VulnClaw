name: CI

on:
  push:
    branches: ["main", "dev"]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install backend deps
        run: python -m pip install --upgrade pip

      - name: Install backend package
        run: pip install -e ".[dev,web]"

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Ruff
        run: ruff check vulnclaw tests

      - name: Frontend typecheck
        working-directory: frontend
        run: npx tsc -b

      - name: Pytest
        if: matrix.os != 'ubuntu-latest' || matrix.python-version != '3.12'
        run: pytest -q

      - name: Pytest with coverage
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        run: pytest -q --cov=vulnclaw --cov-report=xml --cov-report=term-missing

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          files: coverage.xml
          flags: python
          fail_ci_if_error: ${{ github.event_name == 'push' }}
          # Mint an OIDC token only on push to main/dev; PRs (incl. forks)
          # upload unauthenticated so PR-triggered jobs never hold id-token.
          use_oidc: ${{ github.event_name == 'push' }}
